apiVersion: v1
kind: Service
metadata:
  name: rfthz-app-service
  namespace: rfthz-app
spec:
  type: NodePort
  selector:
    app: rfthz-app
  ports:
    - port: 80
      targetPort: 3000
      nodePort: 30080